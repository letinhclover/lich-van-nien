# ============================================================
# robots.txt — Lịch Vạn Niên AI (lichvannien.io.vn)
# ============================================================

# ── Bot chung: cho phép crawl toàn bộ trừ ngoại lệ ──────────
User-agent: *
Allow: /

# Không index API (dynamic, không có nội dung SEO)
Disallow: /api/

# Không index build artifacts của Astro (JS/CSS hash files)
Disallow: /_astro/

# Không index trang cài đặt (noindex cũng có trong meta, nhưng
# robots.txt giúp Googlebot không tốn crawl budget vào đây)
Disallow: /cai-dat

# ── Sitemap: giúp Google và Bing tìm thấy 1825+ trang ngày ──
Sitemap: https://lichvannien.io.vn/sitemap.xml

# ── Chặn AI crawlers (bảo vệ nội dung khỏi training data) ───
# GPTBot: OpenAI crawler dùng để train GPT
User-agent: GPTBot
Disallow: /

# ChatGPT-User: ChatGPT Browse with Bing
User-agent: ChatGPT-User
Disallow: /

# Google Extended: Google Bard/Gemini training crawler
User-agent: Google-Extended
Disallow: /

# CCBot: Common Crawl — nguồn dữ liệu train nhiều LLM
User-agent: CCBot
Disallow: /

# anthropic-ai: Claude training crawler
User-agent: anthropic-ai
Disallow: /

# ── Google Image: chỉ cho crawl thư mục og-image ─────────────
# Cho phép Google Images index ảnh OG để hiển thị trên kết quả
User-agent: Googlebot-Image
Allow: /og-image
